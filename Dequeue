#include <iostream>
using namespace std;

class Deque
{
    int arr[5];
    int front, rear;
    int size;

public:

    Deque()
    {
        size = 5;
        front = -1;
        rear = -1;
    }

    bool isFull()
    {
        return (front == 0 && rear == size - 1) ||
               (front == rear + 1);
    }

    bool isEmpty()
    {
        return front == -1;
    }

    void insertFront(int value)
    {
        if(isFull())
        {
            cout << "Deque Overflow\n";
            return;
        }

        if(front == -1)
        {
            front = rear = 0;
        }
        else if(front == 0)
        {
            front = size - 1;
        }
        else
        {
            front--;
        }

        arr[front] = value;
    }

    void insertRear(int value)
    {
        if(isFull())
        {
            cout << "Deque Overflow\n";
            return;
        }

        if(front == -1)
        {
            front = rear = 0;
